services:

  fatum-redis:
    image: redis
    ports:
      - "6379:6379"

  fatum-database:
    image: postgres:latest
    container_name: fatum_db
    env_file:
      - ./.env
    ports:
      - "5432:5432"

    volumes:
      - ${POSTGRES_VOLUME}:/var/lib/postgresql

