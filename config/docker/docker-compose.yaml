version: "3"
services:
  # Redis for django channels/celery tasks handling
#  redis:
#    image: redis
#    ports:
#      - "6379:6379"

  # Postgres database
  database:
    image: postgres:latest
    container_name: fatum_db
    env_file:
      - ./.env
    ports:
      - "5432:5432"

    volumes:
      - ${POSTGRES_VOLUME}:/var/lib/postgresql

volumes:
  database:
